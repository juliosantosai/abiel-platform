# types

Shared declaration files used during the incremental JavaScript to TypeScript migration.

Guidelines:
- Keep declarations small and module-focused.
- Remove temporary declarations once modules are fully migrated.
- Do not add runtime code here.
